# Trading Dashboard — handoff (end of day 2026-05-02)

## Where we left off

**Phase 1 of the trading dashboard is shipped to production and merged to main.**

- 🌐 Live: https://tradingbot-dashboard-blue.vercel.app
- 📦 Vercel project: `tims-projects-f798c8a6/tradingbot-dashboard`
- 🗄️ KV store: `upstash-kv-red-canvas` (Upstash Redis via Vercel Marketplace)
- ✅ All 34 vitest tests passing, build clean
- ✅ Merged: PR #1 → main on 2026-05-03 03:22 UTC
- ✅ Bot-state push contract proven: 3 manual workflow runs (TSLA Monitor, TSLA Monitor Aggressive, Congress Copy) all succeeded today, KV populated with real state across all 5 keys

## What you can paste into a new Claude session tomorrow

Copy the section below as your opening message:

---

I'm continuing work on the **Trading Dashboard** I built yesterday. Spec at `docs/superpowers/specs/2026-05-02-trading-dashboard-design.md`, Phase 1 plan at `docs/superpowers/plans/2026-05-02-trading-dashboard-phase1.md`. Everything is on `main` now (PR #1 was merged). Live at https://tradingbot-dashboard-blue.vercel.app — a Vite + React 19 + Tailwind v4 SPA in the `dashboard/` subdirectory of this repo.

**Phase 1 is complete and shipped.** Auth (TOTP + backup codes), bot-state push contract (3 GitHub Actions workflows POST to `/api/bot-state` after each cycle), dual-account monitoring (positions + orders + DTE column + wheel-close % progress), and the lookup page (TradingView chart, 25-expiration options chain w/ all 5 Greeks + put/call filter + 6-strike default, Robinhood-style earnings via yfinance, fundamentals, wheelability scoring, news, watchlist add). Equity-curve sparklines on Home cards. ErrorBoundary on every panel.

**What's next is Phase 2: manual trading + AI grading.** From the spec: order placement (stocks: market/limit/stop/stop-limit/trailing; options: single-leg open/close), required entry-grade-with-reasoning on every order, two-step confirm with TOTP re-prompt above per-account `$` threshold, AI hindsight grading on close (Sonnet 4.6 with prompt caching, plain-English no-jargon system prompt), tag system, `/trade/:id` detail page, `/trades` history. See the spec's "Phase 2" section for the full deliverable list.

I'd like to **brainstorm + write the Phase 2 implementation plan** today. Key context to load:

- The Phase 1 plan + spec (paths above)
- The existing `dashboard/api/_lib/` helpers (totp, session, auth-guard, kv, alpaca, alpacaTrade, alpacaData, rate-limit, backup-codes) — Phase 2 will reuse all of them
- The catchall API pattern at `dashboard/api/alpaca/[endpoint].ts` and `dashboard/api/auth/[action].ts`
- The Vercel Hobby 12-function limit (current count: ~6, plenty of room)
- The known SDK quirk: `@alpacahq/typescript-sdk@0.0.32-preview` ignores per-request baseURL overrides — for any new market-data calls, use `alpacaData()` from `dashboard/api/_lib/data-api.ts`. For trading endpoints (orders, account), the SDK works fine.
- The TS strict-syntax rule: `tsconfig.app.json` has `erasableSyntaxOnly: true` — no parameter properties, no enums, no value namespaces. Use explicit field declarations.

Please use the `superpowers:writing-plans` skill to write the Phase 2 plan, organized by milestone (settings page → order form → trade detail → grading → history page). Each task bite-sized with code blocks.

---

## Architecture cheat-sheet (for quick reference tomorrow)

```
dashboard/
├── api/
│   ├── _lib/           helpers — kv, totp, session, auth-guard, alpaca,
│   │                   data-api (alpacaData + alpacaTrade), rate-limit,
│   │                   backup-codes, kv-keys (whitelist)
│   ├── auth/[action].ts            login | logout | session
│   ├── alpaca/[endpoint].ts        account | positions | orders | quote |
│   │                               chain | news | bars | equity-history
│   ├── kv/[resource].ts            bot-state | watchlist
│   ├── bot-state.ts                bot push webhook (bearer-auth)
│   ├── fundamentals.py             yfinance (Python edge function)
│   └── fundamentals-proxy.ts       TS proxy that gates Python with INTERNAL_FUNCTIONS_TOKEN
├── src/
│   ├── routes/         Login · Home · Positions · Orders · Lookup
│   ├── components/
│   │   ├── auth/ProtectedRoute.tsx
│   │   ├── layout/{AppShell, Sidebar}.tsx
│   │   ├── account/{AccountSelector, AccountCard}.tsx
│   │   ├── lookup/{TradingViewChart, QuotePanel, PositionContextPanel,
│   │   │           OptionsChain, WheelabilityPanel, NewsPanel,
│   │   │           EarningsPanel, FundamentalsPanel}.tsx
│   │   ├── ErrorBoundary.tsx
│   │   └── Sparkline.tsx
│   ├── hooks/{useAuth, useAccount, useBotState}.ts
│   └── lib/{api, format, wheelability, option-symbol}.ts
```

## Vercel env vars (already set in production)

- `DASHBOARD_PASSWORD`, `TOTP_SECRET`, `SESSION_SECRET`, `BACKUP_CODES_HASHED`
- `BOT_PUSH_TOKEN` (mirrored to GitHub Actions secret on `tsronco/TradingBotTest-Claude`)
- `INTERNAL_FUNCTIONS_TOKEN` (proxy → Python)
- `ALPACA_API_KEY`, `ALPACA_API_SECRET`, `ALPACA_AGG_API_KEY`, `ALPACA_AGG_API_SECRET`, `ALPACA_BASE_URL`
- `KV_REST_API_URL`, `KV_REST_API_TOKEN`, `KV_URL`, `KV_REST_API_READ_ONLY_TOKEN`, `REDIS_URL` (auto-injected by Marketplace)

## One small thing left lying around

The temp file with all your generated secrets is at `C:\Users\fatti\AppData\Local\Temp\dashboard-deploy-secrets-1777771330.txt`. Once you've confirmed your TOTP app and backup codes are saved somewhere durable (1Password etc.), delete it.

## Good night

Quite a day. Get some rest. 🍻
