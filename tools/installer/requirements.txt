# Optional — only needed for `python setup.py`'s GitHub-secrets push.
# GitHub requires libsodium sealed-box encryption of secret values.
# Everything else in the wizard uses the project's base requirements.
pynacl>=1.5.0
